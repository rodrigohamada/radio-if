<h1 style="text-align:center; margin-bottom:24px;">Nossa Equipe</h1>

<div class="grid cols-3">
  <% (teamMembers || []).forEach(m => { %>
    <div class="card" style="text-align:center; padding:20px;">
      <img src="<%= m.foto_url || '/images/avatar.png' %>"
           alt="Foto de <%= m.nome %>"
           style="width:100%; max-width:220px; border-radius:12px; margin:0 auto 12px; display:block; box-shadow:0 4px 10px rgba(0,0,0,.1)">
      
      <h3 style="margin:10px 0 4px;"><%= m.nome %></h3>
      <p style="color:var(--muted); font-weight:500; margin-bottom:10px;">
        <%= m.funcao || '' %>
      </p>

      <p style="font-size:15px;">
        <% if (m.instagram) { %>
          <a href="<%= m.instagram %>" target="_blank" style="color:var(--primary); text-decoration:none; font-weight:600;">
            📸 Instagram
          </a>
        <% } %>
        <% if (m.instagram && m.linkedin) { %> • <% } %>
        <% if (m.linkedin) { %>
          <a href="<%= m.linkedin %>" target="_blank" style="color:var(--primary); text-decoration:none; font-weight:600;">
            💼 LinkedIn
          </a>
        <% } %>
      </p>
    </div>
  <% }) %>
</div>

<% if (!(teamMembers && teamMembers.length)) { %>
  <p style="text-align:center; color:var(--muted); margin-top:20px;">Nenhum integrante cadastrado ainda.</p>
<% } %>
